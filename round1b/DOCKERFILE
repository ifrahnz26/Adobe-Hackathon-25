Dockerfile

# Use a compatible base image for linux/amd64
FROM --platform=linux/amd64 python:3.9-slim

WORKDIR /app

# Copy requirements file and install dependencies, including torch for CPU
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy the solution code into the container
COPY solution.py .

# When the container runs, it will download the model to a cache inside the container
# on the first run. Subsequent runs will use the cache, allowing offline execution.
